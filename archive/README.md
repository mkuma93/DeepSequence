# Archive

Legacy material moved out of the active DeepSequence product surface (v1.6).

| Path | Contents |
|------|----------|
| `src/` | Older `deepsequence-pwl` package (PWL components + TabNet hierarchical) |
| `models/` | Saved `.keras` checkpoints from earlier experiments |
| `root_legacy/` | Former repo-root packaging (`deepsequence-pwl` 1.0): README, `pyproject.toml`, `setup.py`, etc. |

**Active product:** the [repo root](../) (package `deepsequence_hierarchical_attention/`)

These files are kept for reference only. Do not treat them as the installable product.
