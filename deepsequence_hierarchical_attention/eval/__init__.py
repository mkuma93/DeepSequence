"""Evaluation helpers, classical baselines, and bake-off entry points.

CLI modules (``python -m deepsequence_hierarchical_attention.eval.<name>``):
multihorizon_compare, same_features_compare, weekly_mh, public_carparts,
public_carparts_mh_all, ds_hybrid_mh, ds_hybrid_temporal,
ds_multihorizon_improve, ds_residual_reclaim, train_gate_calibrate,
dump_component_readout.
"""
